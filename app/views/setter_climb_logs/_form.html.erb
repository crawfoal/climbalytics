<%= form_for(@setter_climb_log) do |f| %>
  <% if @setter_climb_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setter_climb_log.errors.count, "error") %> prohibited this setter_climb_log from being saved:</h2>

      <ul>
      <% @setter_climb_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
